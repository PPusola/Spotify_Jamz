node_modules/
.expo/
dist/
npm-debug.*

# Expo prebuild output — generated, do not commit
android/
ios/

# Worker build/cache (worker/ source is committed; this hides its build cache)
worker/.wrangler/
worker/node_modules/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# Environment variables — NEVER commit your real .env
.env

# Claude Code local settings (machine-specific paths and permissions)
.claude/settings.local.json

# macOS
.DS_Store

# Firebase — contains API keys, never commit
google-services.json
GoogleService-Info.plist
